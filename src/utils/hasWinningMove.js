/* 
    checks if a player has a diagonal 
    takes in params from main app
    returns indexs of winning move
    
    (board, player, cups) => [1, 2]
*/

const hasWinningMove = (board, player = 0, cups) => {
  return;
};

export default hasWinningMove;
